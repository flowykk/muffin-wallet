Muffin Currency установлен: {{ .Values.appName }}

Параметры:
  Реплики: {{ .Values.replicaCount }}
  Образ: {{ .Values.image.repository }}:{{ .Values.image.tag }}
  Сервис: {{ .Values.serviceType }} на порту {{ .Values.ports.servicePort }}

Проверить статус:
  kubectl get pods -l app={{ .Values.appName }}

{{- if eq .Values.serviceType "ClusterIP" }}
Доступ к приложению:
  kubectl port-forward service/{{ .Values.appName }} {{ .Values.ports.servicePort }}:{{ .Values.ports.servicePort }}
  curl "http://localhost:{{ .Values.ports.servicePort }}/rate?from=CARAMEL&to=CHOKOLATE"
{{- end }}
